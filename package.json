{
  "name": "togovar-stanza",
  "version": "0.0.1",
  "license": "MIT",
  "repository": "https://github.com/togovar/stanza.git",
  "private": true,
  "engines": {
    "node": ">=14"
  },
  "dependencies": {
    "postcss-import": "^14.0.2",
    "togostanza": "github:togostanza/togostanza#v3.0.0-beta.19"
  },
  "devDependencies": {
    "eslint": "^7.24.0",
    "stylelint": "^13.12.0",
    "stylelint-config-recommended": "^4.0.0",
    "stylelint-config-recommended-scss": "^4.2.0",
    "stylelint-scss": "^3.19.0"
  },
  "scripts": {
    "build": "yarn togostanza build",
    "generate": "yarn togostanza generate stanza",
    "start": "yarn togostanza serve",
    "lint": "yarn lint:js && yarn lint:css",
    "lint:css": "stylelint common.scss stanzas/**/*.{css,scss,sass}",
    "lint:js": "yarn lint:eslint lib stanzas",
    "lint:eslint": "eslint --ignore-path .gitignore"
  }
}
